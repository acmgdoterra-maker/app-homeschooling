interface HabitTrackerProps {
  childId: string
}

export default function HabitTracker({ childId }: HabitTrackerProps) {
  return (
    <div className="text-center py-12">
      <p className="text-gray-400">El rastreador de hábitos está en construcción</p>
    </div>
  )
}
